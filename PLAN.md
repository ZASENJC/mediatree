# MediaTree-NEXT 执行规划

> 本文件是 MediaTree-NEXT 项目的唯一执行基线。所有开发必须遵守根目录 `AGENTS.md`，并在每次工作结束前更新本文件的状态和执行记录。

## 1. 项目目标

以 Jellyfin 服务端为技术基准，建设一个以 MediaTree 品牌发布的开源媒体服务器：

- 使用 Jellyfin 的媒体库扫描、数据库、用户、权限、会话、播放进度、直接播放、HLS、转码、字幕、图片处理和标准 API。
- MediaTree Web、Android、Apple 客户端直接使用 Jellyfin API，不再延续现有 MediaTree 后端 API。
- 保持第三方 Jellyfin 客户端、播放器和挂载工具对电影、剧集和播放主流程的兼容。
- MediaTree 的差异化能力第一阶段优先以 Jellyfin 插件实现；只有在插件边界不合适且经过审查时，才允许合并到服务端核心。
- 从运行资源、用户配置和源码维护三个方向逐步减重，不以删除代码行数作为唯一目标。

## 2. 已确认决策

1. 服务端基于 `jellyfin/jellyfin` fork，不重新开发媒体服务器核心。
2. 不为现有 MediaTree FastAPI API 建兼容层；新前端直接改用 Jellyfin API。
3. Jellyfin 是媒体 Item、用户、会话、进度、播放和媒体库状态的唯一数据权威。
4. 不允许 MediaTree 与 Jellyfin 两套扫描器、两套播放链路或两套主数据库并行写入。
5. 标准 Jellyfin API 的路由、DTO 和语义不得为 MediaTree 定制功能而修改。
6. MediaTree 自定义插件端点统一使用独立命名空间，例如 `/MediaTree/*`。
7. 插件加载能力必须保留；官方插件商店、在线安装或更新能力是否保留，在第二部分审计后决定。
8. 第一部分由 Codex 按本规划完整实施；第二部分必须先完成 Jellyfin 服务端源码审计、提交删减清单和批次路线，再进入删除与迁移。
9. 服务端衍生代码遵守 Jellyfin 的 GPL-2.0 要求；MediaTree 名称、图标和资源使用自己的品牌。

## 3. 当前假设与待确认项

已确认的仓库与工作区边界如下；仍有待确认事项会阻塞对应实施步骤：

- 服务端工作名称使用 `MediaTree Server`。
- 当前工作区为 `/Volumes/STU/Code/mediatree-NEXT`，远端 `origin` 固定为 `https://github.com/ZASENJC/mediatree.git`。
- 所有本项目迁移提交都在本地 `next` 分支创建，并只推送到远端 `next`；不得提交或推送到 `main`。
- 当前仓库是 MediaTree 现有代码和新服务端迁移的集成开发线，不再创建或使用独立的 `ZASENJC/mediatree-server` 仓库。
- 在 P1.0 建立 Jellyfin 服务端基线时，才将 `jellyfin/jellyfin` 以 `upstream` 远端接入，并记录固定上游 commit、历史保留方式和同步策略。
- 现有 MediaTree Web、Android 和 Apple 客户端在本仓库或其既有客户端仓库中逐步改用 Jellyfin API；旧 FastAPI 后端的删除或迁移必须遵循第二部分审查闸门。
- Android 和 Apple 客户端保留各自的 provider 边界；新服务端通过 Jellyfin provider 接入，不把 MediaTree、Jellyfin/Emby、SMB、WebDAV 的实现混为一套。
- 第一阶段交付以 Docker 为主要服务端运行方式；其他安装包不在当前规划内。

待确认项如果会改变仓库、发布目标、许可证或客户端范围，必须先获得用户确认，不能由执行者自行猜测。

## 4. 全局架构约束

### 4.1 必须保持

- Jellyfin 标准鉴权、Item、Library、UserData、Session、PlaybackInfo、Stream、Image 和 WebSocket 协议。
- 电影、剧集、季、集以及相关播放能力。
- FFmpeg 探测、直接播放、转封装、HLS 和必要的转码能力。
- 用户、权限、播放进度、收藏和会话同步。
- 插件加载、插件 DI 注册和插件 API Controller 扩展能力。
- 数据库迁移和现有数据可升级能力。

### 4.2 明确禁止

- 恢复或继续扩展现有 MediaTree FastAPI API。
- 为兼容旧 MediaTree API 建立长期 BFF 或双协议网关。
- 直接读写 Jellyfin 内部数据库来绕过服务或插件接口。
- 同时运行 MediaTree 和 Jellyfin 的媒体扫描器管理同一媒体库。
- 为了改名而一次性重命名全部 Jellyfin namespace、DTO 或数据库对象。
- 在没有完成第二部分源码审计前删除 Jellyfin 项目、Controller、服务、实体或迁移。
- 以隐藏前端入口代替已声明完成的物理删减，或反过来把未验证的源码删除宣称为运行时优化。

## 5. 第一部分：Fork Jellyfin 并打通前后端

第一部分由 Codex 负责完整实施。目标是先得到一个可运行、可播放、使用 MediaTree 前端且保持 Jellyfin API 兼容的 MediaTree Server。此阶段不进行大规模服务端源码删除。

### P1.0 项目初始化与基线

状态：`进行中`

已完成：已建立 `MediaTree-NEXT` 工作区，`origin` 已连接至 `ZASENJC/mediatree`，本地 `next` 已从 `origin/main` 创建。尚未导入 Jellyfin 源码、添加 `upstream` 或执行构建基线。

任务：

- 确认当前 `mediatree` 仓库的 `next` 为唯一迁移开发线，并保留其现有历史。
- fork 或导入 `jellyfin/jellyfin`，记录准确的上游 commit 和版本。
- 在建立 Jellyfin 服务端基线时配置 `upstream`，制定上游同步方式。
- 检查仓库原有 `AGENTS.md`、贡献规则、构建脚本、许可证和发布流程。
- 保留完整 Git 历史、版权声明和 GPL-2.0 许可证。
- 记录未修改 Jellyfin 的构建、启动、Docker 镜像大小和空闲资源基线。
- 不在本步骤进行 namespace 大改名或功能删除。

完成标准：

- 服务端可从固定上游 commit 构建并启动。
- 原生 Jellyfin Web 或标准客户端可完成登录、浏览和播放基线冒烟。
- `origin`、`upstream`（建立后）和同步说明写入项目文档。
- 基线结果记录在本规划执行记录或专门基线文档中。

### P1.1 MediaTree 品牌与发布身份

状态：`待开始`

任务：

- 使用 MediaTree 名称、图标、包名、镜像名和文档，不复用 Jellyfin logo。
- 区分产品展示身份和协议兼容字段；不能为了改名破坏第三方客户端协议判断。
- 增加清晰的上游归属、GPL-2.0 说明和修改说明。
- 修复现有 MediaTree 仓库 README 声称 MIT 但缺少实际 `LICENSE` 的问题；前端是否保持 MIT 必须按代码边界和依赖审计结果执行。
- 不在完成 P1.0 基线并确认发布范围前创建 Release。

完成标准：

- 用户界面和发布产物不冒充 Jellyfin 官方项目。
- 服务端许可证、上游归属和源码获取方式完整。
- 标准 Jellyfin API 兼容字段未被无依据修改。

### P1.2 Jellyfin API 使用清单与前端领域模型

状态：`待开始`

任务：

- 从 MediaTree Web 当前页面和工作流生成需求清单，而不是机械映射旧 API。
- 确认登录、服务发现、媒体库、首页、搜索、详情、季/集、图片、收藏、进度、播放信息、字幕和会话所需的 Jellyfin API。
- 为前端建立 Jellyfin API client 和 MediaTree UI 领域适配层。
- 使用 Jellyfin GUID，不继续使用旧 MediaTree 数值 movie ID。
- 明确标准 API 与将来的 `/MediaTree/*` 插件 API 边界。
- 删除前端对旧 MediaTree API 的新增依赖；旧实现按页面迁移进度逐步退役。

完成标准：

- API 清单覆盖第一部分所有验收工作流。
- 前端组件不直接散落拼接 Jellyfin URL、鉴权头和 DTO 转换。
- 标准协议与 MediaTree 自定义能力有明确代码边界。

### P1.3 登录、媒体库和浏览链路

状态：`待开始`

任务：

- 接入 Jellyfin 服务器地址、公开信息和登录流程。
- 安全保存 access token、用户和服务器身份，不保存不必要的密码副本。
- 接入媒体库、首页推荐、继续观看、搜索、收藏和文件夹/季集浏览。
- 将 MediaTree 前端当前的 Movie/Folder 模型映射到 Jellyfin Item DTO。
- 处理空库、无权限、离线、token 过期和服务器版本不兼容状态。
- 设置页只暴露当前用户真正需要的基础设置；高级 Jellyfin 设置暂时保留在服务端，但不作为主流程入口。

完成标准：

- 新用户可以连接 MediaTree Server、登录并浏览电影和剧集。
- 登录态恢复和退出行为正确。
- 不依赖旧 MediaTree FastAPI 服务即可完成浏览主流程。

### P1.4 播放、字幕和进度链路

状态：`待开始`

任务：

- 使用 Jellyfin `PlaybackInfo` 和媒体源选择，不自行猜测是否转码。
- 优先直接播放；由 Jellyfin 决定转封装、HLS 或完整转码。
- 接入音轨、字幕轨、外部字幕、播放起点和播放进度上报。
- 保留 MediaTree 已有的播放器体验，但播放资源和会话完全使用 Jellyfin 协议。
- 保留已有的宽格式字幕策略，不能因为 API 切换而退化 ASS/SSA 或错误编码处理。
- 外部播放器链接必须建立在 Jellyfin 的鉴权和流媒体 URL 规则上。

完成标准：

- 代表性 MP4 和 MKV 可播放。
- 至少覆盖直接播放和一次确实需要的转码/HLS 路径。
- 字幕选择、继续播放、暂停/停止进度和播放完成状态可用。
- 不再调用旧 `/api/stream/*`、`/api/media-info/*` 或旧进度接口。

### P1.5 极简初始配置和管理入口

状态：`待开始`

任务：

- 设计 MediaTree 首次设置流程，只包含运行所需的服务器、管理员、媒体库和基础语言/地区设置。
- 为常见家庭使用场景提供安全默认值。
- 把高级网络、转码、元数据和权限选项放入高级设置，不在首屏堆叠。
- 第一部分以隐藏和重组配置为主，不物理删除底层服务端配置。
- 为第二部分记录用户仍看得到但不需要的 Jellyfin 配置项。

完成标准：

- 新安装可以通过 MediaTree 界面完成首次设置。
- 常规用户不需要进入 Jellyfin 原始 Dashboard 才能添加媒体库和开始播放。
- 高级设置仍有可恢复入口，避免在源码删减前失去排障能力。

### P1.6 插件边界脚手架

状态：`待开始`

任务：

- 建立独立的 MediaTree Jellyfin 插件工程和版本规则。
- 验证插件可以注册服务、读取 Jellyfin Item、注册 `/MediaTree/*` API，并拥有隔离的数据目录。
- 仅加入能力探测等最小脚手架，不在第一部分提前移植全部 MediaTree 功能。
- 插件不得复制 Jellyfin 主数据库，不得接管用户、扫描或播放。

完成标准：

- 插件可以独立构建、安装、启用和禁用。
- 禁用插件后标准 Jellyfin API 和第三方客户端仍可使用。
- 自定义端点不会覆盖或改变标准 Jellyfin 路由。

### P1.7 打包和第三方兼容

状态：`待开始`

任务：

- 将 MediaTree Web 构建产物与 MediaTree Server Docker 发布方式打通。
- 保持前后端可独立开发，发布时提供一个直观的默认入口。
- 使用标准 Jellyfin 客户端或 SDK 验证登录、Item、图片、PlaybackInfo、流媒体和进度协议。
- 选择一个用户实际使用的第三方 Jellyfin 播放器或挂载工具做代表性冒烟，不遍历所有客户端。
- 记录兼容范围：电影、剧集、季集、图片、直接播放、转码和进度；被删减的可选功能不承诺兼容。

完成标准：

- Docker 新安装可打开 MediaTree 界面完成设置并播放。
- MediaTree Web 不依赖旧 Python 后端。
- 代表性第三方 Jellyfin 客户端能连接、浏览并播放。
- 发布物、源码和许可证相互对应。

### 第一部分总体验收

只有同时满足以下条件，第一部分才可标记完成：

- MediaTree Server 基于固定 Jellyfin 上游版本可重复构建。
- MediaTree Web 完成登录、浏览、搜索、详情、季集、收藏、播放、字幕和进度主流程。
- Jellyfin 标准 API 未被 MediaTree 自定义功能破坏。
- 至少一个标准/第三方 Jellyfin 客户端完成代表性连接与播放冒烟。
- 旧 MediaTree FastAPI 不再是新服务端运行依赖。
- MediaTree 插件脚手架可独立启停。
- 配置流程已明显简化，但尚未通过危险源码删除来实现。
- 规划、架构、许可证和部署文档已更新。

## 6. 第二部分：源码删减与 MediaTree 功能插件迁移

第二部分的原则是“先完全理解，再提出删减，再经审查执行”。任何人不得把目录名或项目名当作可删除证明。

### P2.0 Jellyfin 服务端完整源码审计

状态：`第一部分完成后开始`

必须产出 `docs/JELLYFIN_SOURCE_AUDIT.md`，至少覆盖：

- Solution、项目引用和 NuGet 依赖关系。
- `Program`、Host、Startup、DI 注册、反射发现和生命周期。
- 标准 API Controller、DTO、鉴权策略及第三方客户端依赖面。
- 用户、权限、会话、播放进度和设备 Profile。
- 媒体库扫描、命名解析、监控、Item Resolver 和元数据 Provider。
- SQLite/EF Core 实体、仓库、迁移、备份和升级路径。
- FFmpeg 探测、媒体源协商、直接播放、转封装、HLS、完整转码和硬件加速。
- 图片、字幕、附件、Trickplay、章节和媒体片段。
- Live TV、DVR、Channels、SyncPlay、Quick Connect、DLNA 等候选功能的完整调用链。
- 插件发现、加载上下文、DI、Controller、配置页、安装和更新机制。
- 后台任务、事件、WebSocket、健康检查、指标和日志。
- Docker、打包、运行依赖、默认配置和资源占用来源。
- 对应单元/集成测试以及各模块的最小验证入口。

审计规则：

- 必须阅读导出、调用者、共享接口和测试，不得只看文件夹名称。
- 每个结论必须有文件/类型/调用路径证据。
- 必须区分“未注册即可停用”“可以删除叶子模块”“与核心模型耦合”“不确定”。
- 在审计文档完成前，不允许执行第二部分源码删除。

### P2.1 运行资源与配置复杂度基线

状态：`等待 P2.0`

建立一次可重复的代表性基线：

- Docker 镜像大小和主要层占用。
- 冷启动时间与空闲 RSS。
- 一次代表性媒体库扫描的时间和峰值内存。
- 一次直接播放和一次转码的 CPU/内存。
- 首次设置步骤、常规设置项数量和用户必须理解的概念。
- 项目数、直接依赖数和候选模块依赖扇出。

这些测量属于第二部分必要验证，不算额外测试。不得为了追求完整覆盖增加多设备、多媒体库或长时间压力测试，除非结果不足以支持删减决定。

### P2.2 删减候选矩阵和用户审查闸门

状态：`等待 P2.0-P2.1`

必须产出 `docs/FEATURE_REDUCTION_MATRIX.md`，每项标记为：

- `保留核心`
- `保留插件基础`
- `默认关闭`
- `迁移插件`
- `候选删除`
- `暂缓/证据不足`

当前仅作为审计起点、不是删除授权的候选项：

| 功能 | 初步方向 | 审计重点 |
|---|---|---|
| Live TV、DVR、节目指南 | 候选删除 | API、权限、录制任务、实体和媒体模型引用 |
| 音乐、图书、照片 | 候选删除 | BaseItem 层级、Provider、搜索和客户端返回类型 |
| SyncPlay | 候选删除 | Session、WebSocket、权限和 API |
| Quick Connect | 候选删除 | 登录流程和第三方客户端是否依赖 |
| Channels | 候选删除 | 媒体源、动态 Item 和插件接口 |
| DLNA 发现/投送 | 候选删除 | 发现服务与播放设备 Profile 必须分开判断 |
| 官方插件商店/在线更新 | 候选删除或替换 | 保留本地插件加载和 MediaTree 插件升级能力 |
| 原生 Jellyfin Web/Dashboard | 替换主入口 | 保留必要管理与故障恢复能力 |
| 备份、指标、健康检查 | 暂缓 | MediaTree 运维与容器健康需求 |
| FFmpeg/HLS/字幕/图片 | 保留核心 | 只做依赖和配置减重，不做功能性删除 |

审查闸门：

- 向用户提交源码审计、实测基线、候选矩阵、预期收益、兼容影响、回滚方案和执行批次。
- 未得到用户对删减范围和批次路线的明确同意，不开始 P2.4 的物理删除。
- 审查后确认的矩阵必须回写本规划，不能只存在聊天记录中。

### P2.3 MediaTree 功能迁移设计

状态：`等待 P2.0`

必须产出 `docs/MEDIATREE_PLUGIN_MIGRATION.md`，逐项核对现有 MediaTree 功能并分类：

- Jellyfin 标准能力已满足：前端直接使用，不开发插件。
- 纯前端体验：保留在 MediaTree Web/客户端。
- 适合 Jellyfin C# 插件：进入插件迁移。
- 只能临时使用外部服务：列出退出外部服务和最终轻量化路线。
- 不再需要：说明移除理由和数据处理方式。

插件迁移的初步优先级：

1. Bangumi、Javdatabase 和 MediaTree 定制 TMDB 元数据 Provider。
2. `sp` 特别篇和 MediaTree 文件夹展示语义。
3. 文件夹海报、背景、标题 logo 和剧集静帧规则。
4. 刮削候选、人工审核和重新刮削工作流。
5. MediaTree 特有分类、标签和附加字段。
6. 无法由 Jellyfin 标准播放链路提供的外部播放器体验。

插件数据规则：

- 优先写入 Jellyfin 支持的标准 Item 元数据和 UserData。
- 插件独有数据存储在插件数据目录，并使用 Jellyfin Item GUID 关联。
- 不创建第二份完整媒体库、用户库、播放进度库或媒体路径索引。
- 插件禁用后，标准电影/剧集浏览和播放仍应工作。
- 调研通用能力时先检查成熟、活跃、许可证兼容的 Jellyfin 插件，不重复手写已有实现。

### P2.4 分批删减执行路线

状态：`等待用户通过 P2.2 审查`

每个批次单独实施、验证、记录和提交，不进行跨批次清理：

#### R1：配置和运行注册减重

- 先通过默认配置、服务注册和功能开关停用候选能力。
- 不立即删除模型和共享接口。
- 对比空闲资源、启动日志和设置复杂度。
- 确认第三方电影/剧集浏览与播放不受影响。

#### R2：独立叶子功能删除

- 优先处理项目边界清晰且调用链已完整审计的功能。
- 同批删除其 API、DI、后台任务、配置、资源、依赖和测试。
- 第一候选通常为 Live TV/DVR，但以 P2.2 通过后的矩阵为准。

#### R3：非目标媒体类型删除

- 按已批准范围处理音乐、图书、照片等媒体类型。
- 先解决 BaseItem、搜索、Provider 和 DTO 共享依赖，不能只删 Controller。
- 明确第三方客户端看到空能力还是不支持响应。

#### R4：协作、连接和发现能力删除

- 按批准范围处理 SyncPlay、Quick Connect、Channels 和 DLNA 发现/投送。
- 保留仍用于播放能力协商的设备 Profile 和媒体源模型。

#### R5：插件和更新体系收敛

- 保留 MediaTree 插件加载、配置和升级所需的最小基础。
- 按批准方案移除官方仓库、无关安装 UI 或重复更新链路。
- 确保插件失败不会影响标准服务端启动和播放。

#### R6：依赖、镜像和发布减重

- 删除已无引用的 NuGet、资源、原生库和镜像层。
- 简化默认配置和发布包内容。
- 用 P2.1 同一口径复测镜像、空闲、扫描和播放指标。

#### R7：源码结构收敛

- 只在功能删减稳定后处理项目合并、目录调整或公共接口收敛。
- 不为了代码看起来更短进行大规模重命名。
- 保留清晰的上游差异说明和后续安全更新路径。

### P2.5 现有 MediaTree 数据迁移

状态：`等待 P2.3`

任务：

- 迁移前备份现有 MediaTree SQLite 和数据目录。
- 按规范化媒体路径把旧记录映射到 Jellyfin Item GUID。
- 评估迁移播放进度、收藏、分类、标签、刮削结果、图片和特别篇标记。
- 用户密码不直接搬运到 Jellyfin，除非经过安全审计且格式明确兼容。
- 迁移必须可重复检测、可中断并输出未匹配记录，不能静默跳过。
- 迁移完成后停止旧数据库写入；旧数据只作为只读回滚来源保留一段明确周期。

### 第二部分总体验收

- 完成并经用户审查的源码审计、基线、删减矩阵和插件迁移方案均在仓库内。
- 每个批准的删减批次都有依赖证据、最小验证和前后指标。
- MediaTree 核心特色已按标准能力、前端或插件明确归属。
- 标准 Jellyfin 电影/剧集 API 和代表性第三方客户端仍能浏览与播放。
- 不存在双扫描、双主数据库、双用户系统或双播放链路。
- 首次设置和常规配置显著简化，用户无需理解无关 Jellyfin 功能。
- 镜像、空闲资源和源码依赖相对 P2.1 基线有可量化改善。
- 插件与核心边界有文档；后续是否合并到核心按实际限制决定，而非提前决定。

## 7. 最小必要验证策略

本项目不做多余测试。验证强度只随当前变更风险增加：

- 文档或规则变更：只检查文件内容、链接和 diff。
- 单一插件或模块变更：构建受影响项目，运行直接相关测试，做一条相关冒烟。
- 前端 API 页面迁移：运行前端构建和该工作流的最小手动/自动冒烟。
- 服务端删减：构建受影响解决方案，运行直接相关测试，并验证登录、浏览或播放中受影响的最小链路。
- 发布前：只执行规划中明确列出的构建、代表性端到端主流程和第三方兼容冒烟。
- P2.1 与每个删减批次的资源复测属于必要验收，不扩展成长时间压力测试。

默认禁止：

- 因为“顺便放心”而运行全量测试矩阵。
- 同时测试所有操作系统、设备和客户端。
- 与本次改动无关的浏览器截图、视觉回归或性能测试。
- 已有充分证据后重复执行同一验证。
- 用大量无关测试掩盖关键主流程没有验证。

无法运行必要验证时，必须如实记录为未验证，不得用其他测试替代后宣称完成。

## 8. 计划外操作审批格式

如果需要进行本规划未包含的写操作、依赖引入、架构变化、仓库调整、部署、发布或破坏性操作，必须暂停并向用户提交：

1. 要解决的问题。
2. 为什么现有规划无法覆盖。
3. 建议方案和备选方案。
4. 涉及仓库、文件和外部资源。
5. 对兼容、许可证、数据和上游同步的影响。
6. 最小验证方式。
7. 回滚方式。

获得用户明确同意后，先把批准内容加入本规划，再开始执行。禁止事后修改规划为已经发生的计划外操作补授权。

## 9. 当前状态

| 项目 | 状态 | 说明 |
|---|---|---|
| 规划与代理规则 | 完成 | 初始两部分规划已调整为 `MediaTree-NEXT`；`AGENTS.md` 已写入分支、审批和最小验证规则 |
| 第一部分 | P1.0 进行中 | 已确认 `ZASENJC/mediatree` 为 `origin`，迁移开发线为 `next`；尚未建立 Jellyfin 服务端基线 |
| 第二部分源码审计 | 未开始 | 第一部分完成后开始 |
| 第二部分删减 | 禁止执行 | 必须通过 P2.2 用户审查闸门 |
| 插件迁移 | 未开始 | P1.6 只创建脚手架，功能迁移在 P2.3 后实施 |

## 10. 每次运行执行记录

每次工作结束前必须追加或更新一行，不能只在对话中报告。

| 日期 | 阶段 | 本次完成 | 验证 | 剩余/阻塞 | 下一步 |
|---|---|---|---|---|---|
| 2026-07-31 | 规划 | 创建两部分执行规划和治理规则；已完成文档级复核 | 检查两份文件的结构、关键约束、审批闸门，以及无尾随空白/Tab | 远端仓库名称、布局待用户确认；未执行 fork、克隆或构建 | 用户审查规划后进入 P1.0 |
| 2026-07-31 | P1.0 初始化 | 工作区从 `/Volumes/STU/Code/jellyfin` 重命名为 `/Volumes/STU/Code/mediatree-NEXT`；`origin` 连接到 `ZASENJC/mediatree`；从 `origin/main` 创建本地 `next`；更新分支和执行规则，并将规则与规划提交到 `next` | 已核对当前目录、`next`、`origin` URL、`origin/main` 基线 commit 及文档中旧路径/远端引用；仅执行文档格式和 Git 状态检查，未运行构建或测试 | 尚未导入 Jellyfin、添加 `upstream` 或运行构建基线；第二部分仍受审批闸门约束 | 推送 `next` 建立远端跟踪分支，再建立 Jellyfin 服务端来源与基线 |
